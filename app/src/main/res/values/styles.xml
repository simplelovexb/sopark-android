<resources>

    <!-- Base application theme. -->


    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="baseTextTheme" >
        <!-- Customize your theme here. -->
        <item name="android:textStyle">normal</item>

    </style>
    <style name="normalText" parent="baseTextTheme">
        <item name="android:textColor">@color/normalText</item>
        <item name="android:textSize">@dimen/font_26</item>
    </style>
    <style name="greyLittleText" parent="baseTextTheme">
        <item name="android:textColor">@color/greyText</item>
        <item name="android:textSize">@dimen/font_22</item>
    </style>
</resources>
